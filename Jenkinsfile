def 
pipeline {
  agent any
  stages {
    stage('') {
      steps {
        script {
          def browsers = ['chrome', 'firefox']
            echo "Testing the ${browsers[i]} browser"
        }
      }
    }
  }
}
