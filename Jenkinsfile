pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh 'def alljob = JOB_NAME.tokenize('/') as String[]
            def proj_name = alljob[1]'
        sh 'echo proj_name'
      }
    }
  }
}
